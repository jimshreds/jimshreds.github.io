---
layout: none
permalink: /feed.podcast.xml
---
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0"
     xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd"
     xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ site.title | xml_escape }}</title>
    <link>{{ site.url | append: site.baseurl }}</link>
    <description>{{ site.description | xml_escape }}</description>
    <language>{{ site.language | default: "en-us" }}</language>
    <atom:link href="{{ site.url | append: site.baseurl | append: "/feed.podcast.xml" }}" rel="self" type="application/rss+xml" />
    <itunes:author>{{ site.title | xml_escape }}</itunes:author>
    <itunes:owner>
      <itunes:name>{{ site.title | xml_escape }}</itunes:name>
      <itunes:email>{{ site.email | default: site.github.email }}</itunes:email>
    </itunes:owner>

    {% comment %}Loop site.posts and include any post that has category 'radio'.
       We check for several common representations so it's robust against
       `categories: radio` (string) or `categories: [radio]` (array).
       The item include will render gracefully even when `audio_url` is missing.
    {% endcomment %}
    {% for post in site.posts %}
      {% assign cats = post.categories %}
      {% assign is_radio = false %}
      {% if cats == 'radio' or cats contains 'radio' or post.category == 'radio' %}
        {% assign is_radio = true %}
      {% endif %}
      {% if is_radio and post.audio_url %}
        {% include podcast_item.xml post=post %}
      {% endif %}
    {% endfor %}
  </channel>
</rss>
